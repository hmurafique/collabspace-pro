import React from 'react';
export default function App(){
  return (
    <div style={{padding:20}}>
      <h1>CollabSpace Pro</h1>
      <p>Frontend (Vite) — minimal demo. Hit <code>/api/health</code> on backend to test.</p>
    </div>
  );
}
